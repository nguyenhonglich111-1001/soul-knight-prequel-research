[pytest]
testpaths = tests
# `slow` tests rebuild real outputs from extracted/ and compare them with git HEAD; the
# commit hook skips them. Run everything with:  python -m pytest -m ""
addopts = -m "not slow"
markers =
    slow: rebuilds real pipeline outputs (seconds to minutes)
